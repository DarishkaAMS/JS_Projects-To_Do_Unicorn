"use strict";

const TodoMainUseCaseError = require("./todo-main-use-case-error.js");
const TRYOUT_MAIN_ERROR_PREFIX = `${TodoMainUseCaseError.ERROR_PREFIX}tryoutMain/`;

const SecondTryout = {
  UC_CODE: `${TRYOUT_MAIN_ERROR_PREFIX}secondTryout/`,
  
};

module.exports = {
  SecondTryout
};
